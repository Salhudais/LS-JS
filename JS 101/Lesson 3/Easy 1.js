/*
Q2 -> str.endsWith("!");

Q3 -> ages.hasOwnProperty("Spot");

Q4 -> munstersDescription[0].toUpperCase() + munsterDescription.slice(1);

q6 -> Object.assign(ages, additionalAges);

q7 -> str.includes('Dino');
      str.indexOf('Dino') > -1;
      str.match('Dino') !== null;

q8 -> push, concat or index flintstones[flint.length]

q10 -> advice = advice.substring(0, advice.indexOf("house"));
      advice = slice(0, advice.indexOf("house"));
*/