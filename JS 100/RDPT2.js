// Documentation PT2

const { CommandFailedEvent } = require("mongodb")

1. Violations:
    a) Camelcase
    b) semicolon endings
    c) two - space indentation
    d) keyword spacing 
    e) + spacing 
2. string, number, boolean, BigInt, Symbol, null, undefined
3. Number.MAX_VALUE;
4. 20.9
5. Date.now() -> returns number of milliseconds elapsed 
   since epoch Jan 1, 1970
6. today.getFullyear(), getYear() deprecated
7. Optional separator, omitting separator joins by comma 
8. String.prototype.concat() method, given n str parameters
9. Missing an opening bracket
10. Length is a string property, no need for ()

