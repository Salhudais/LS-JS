// Reading & Documentation Exercises:
// MDN
// String.prototype.toLowerCase
let capString = String[0].toUpperCase() + String.slice(1);
// arr[1];
/* Accessing out of bounds does not produce an error due to
JS's forgiving nature but will return undefined */
// palm tree, sequoia
// string return type identifying the primitive type
// string, object, boolean
// '...  it... of hang the get to Starting'
// True, False


